import { FormCard } from '../shared/ui/FormCard'

export function OtherTabNotice() {
  return (
    <FormCard
      title="Приложение открыто в другой вкладке"
      subtitle="Две вкладки одновременно читали бы одну очередь сообщений и затирали бы переписку друг друга. Закройте другую вкладку — эта подхватит работу сама."
    />
  )
}
